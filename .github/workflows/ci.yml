name: Playwright Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-ui:
    name: UI Tests (SauceDemo)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run UI tests
        run: npm run test:ui

      - name: Upload UI Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-ui
          path: playwright-report/
          retention-days: 14

      - name: Upload UI Monocart report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: monocart-report-ui
          path: monocart-report/
          retention-days: 14

  test-api:
    name: API Tests (Restful-Booker)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Run API tests
        run: npm run test:api

      - name: Upload API Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-api
          path: playwright-report/
          retention-days: 14

      - name: Upload API Monocart report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: monocart-report-api
          path: monocart-report/
          retention-days: 14

  notify:
    name: Send Test Reports
    runs-on: ubuntu-latest
    needs: [test-ui, test-api]
    if: always()

    steps:
      - name: Download UI Playwright report
        uses: actions/download-artifact@v4
        with:
          name: playwright-report-ui
          path: reports/playwright-ui/

      - name: Download UI Monocart report
        uses: actions/download-artifact@v4
        with:
          name: monocart-report-ui
          path: reports/monocart-ui/

      - name: Download API Playwright report
        uses: actions/download-artifact@v4
        with:
          name: playwright-report-api
          path: reports/playwright-api/

      - name: Download API Monocart report
        uses: actions/download-artifact@v4
        with:
          name: monocart-report-api
          path: reports/monocart-api/

      - name: Rename reports for email
        run: |
          cp reports/playwright-ui/index.html reports/playwright-report-ui.html
          cp reports/monocart-ui/index.html reports/monocart-report-ui.html
          cp reports/playwright-api/index.html reports/playwright-report-api.html
          cp reports/monocart-api/index.html reports/monocart-report-api.html

      - name: Send combined test report email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "${{ (needs.test-ui.result == 'success' && needs.test-api.result == 'success') && '✅ All Tests Passed' || '❌ Tests Failed' }} – ${{ github.repository }} (${{ github.ref_name }})"
          to: ${{ secrets.MAIL_TO }}
          from: ${{ secrets.MAIL_USERNAME }}
          body: |
            UI Tests:  ${{ needs.test-ui.result }}
            API Tests: ${{ needs.test-api.result }}

            Branch: ${{ github.ref_name }}
            Commit: ${{ github.sha }}
            Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          attachments: >-
            reports/playwright-report-ui.html,
            reports/monocart-report-ui.html,
            reports/playwright-report-api.html,
            reports/monocart-report-api.html